import React from "react";

const IndexView = () => {
  return (
    <section
      id="index"
      className="h-screen w-full flex justify-center items-start snap-always snap-center"
    >
      <div className="h-40 w-[30rem] shadow-lg bg-primary-100 dark:bg-primary-400 rounded-3xl p-8 mt-20">
        <h1 className="text-3xl font-semibold">Hi, I'm Maneth.</h1>
      </div>
    </section>
  );
};

export default IndexView;
